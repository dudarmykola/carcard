import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Car extends Component {
    static propTypes = {

    };

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                Car
            </div>
        );
    }
}

export default Car;
